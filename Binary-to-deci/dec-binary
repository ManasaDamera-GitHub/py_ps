
# 1) WAP to convert the decimal to binary
# input: Enter a number: 5
# output: 101


number = int(input("Enter decimal number: "))
binary = ""
while number >0:
    remainder = number % 2  # to store remainders (0,1)
    binary = str(remainder) + binary #The remainders are concatenated in reverse order to form the binary number.
    number = number // 2   # n // 2 reduces the number by dividing it by 2.
print("The binary format  is: {}".format( binary))